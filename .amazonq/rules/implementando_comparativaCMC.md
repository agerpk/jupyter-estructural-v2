docs/implementar_comparativaCMC.md must be read once, then at the end of each step that makes changes, must be updated to know the current advancement of the implementation.

QA testing in progress. New problems must be written, then when fixed marked as "testing pendiente", then if resolved (user qa test input) marked as solved.